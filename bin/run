#!/usr/bin/env ruby

require_relative '../lib/runner'
require 'json'

puts '--> start runner'
data = Runner.new(
  btc: BTCScraper.new
).run

puts '--> write to runner-data.json'
File.write('runner-data.json', JSON.dump(data))
puts '--> done'
